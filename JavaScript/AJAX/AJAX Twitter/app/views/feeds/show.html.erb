<h1>Welcome, <span class="username-highlight"><%= link_to  current_user.username, current_user %></span>!</h1>

<article id="feed">
  <h2>Your Feed!</h2>
  <ul>
    <% @feed_tweets.each do |tweet| %>
      <li class="tweet"><%= render 'tweets/tweet', tweet: tweet %></li>
    <% end %>
  </ul>
</article>

<%= render 'tweets/form' %>
