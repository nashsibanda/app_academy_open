<% follow_state = (current_user.follows?(user) ? "followed" : "unfollowed") %>

<button class="follow-toggle form-button" data-user-id="<%= user.id %>" data-initial-follow-state="<%= follow_state %>"></button>
